{
    "name": "@pulumi/aws-quickstart-vpc",
    "version": "${VERSION}",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource aws-quickstart-vpc ${VERSION}"
    },
    "dependencies": {
        "@pulumi/aws": "^4.5.1"
    },
    "devDependencies": {
        "typescript": "^3.7.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    },
    "pulumi": {
        "resource": true
    }
}
